import React from 'react';

const AddTodo = () => {
    return (
        <div>
            <form>
                <input type="text" />
                <button>Add Todo</button>
            </form>
        </div>
    );
}

export default AddTodo;